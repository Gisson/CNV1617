before_install:
  - sudo apt-get -qq update
  - sudo apt-get install make tree -y

language: java
jdk:
  - oraclejdk8

git:
  depth: 3

before_script:
  - make -v
  - tree
  - cat /etc/os-release

script:
  - cd server && make clean all
